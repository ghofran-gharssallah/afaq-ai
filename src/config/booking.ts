/**
 * Calendly booking link — the single place every "Book a Call" CTA reads
 * its destination from. Update here only; nothing else should hard-code
 * this URL.
 */
export const BOOKING_URL = "https://calendly.com/afaq-ai-team/30min";
